const initialState = {
    products : [

    ]
}


export default function name(state = initialState, action) {
    switch (action.type) {
        case "value":
            
            return{
                state
            }
    
        default:
            return{
                state
            }
    }
}